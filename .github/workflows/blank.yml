name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Sync
        uses: swethagodasu/Bucket-sync@v0
#         with:
#           secrets: ${{ secrets.google_service_account_credentials }}
#           bucket: 'bucket-sync-99'
#           exclude: '.*\.md$|\.gitignore$|\.git/.*$|\.github/.*$'
